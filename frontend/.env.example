# Destino del proxy del servidor de desarrollo. Lo lee vite.config.ts y nunca llega al navegador.
# El valor sale del output FunctionUrl de `sam deploy`.
API_PROXY_TARGET=https://xxxxxxxx.lambda-url.us-east-1.on.aws
